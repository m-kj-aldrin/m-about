---
interface Props {
  id: number;
}

const { id } = Astro.props;

import { getCtx, setCtx } from "@lib/vv";
const ctx = getCtx();
const store = {
  name: `Shape: ${id}`,
  children: [],
};

ctx.children.push(store);

setTimeout(() => {
  setCtx(store);
}, 0);
---

<slot />
