---
import { getCtx } from "@lib/vv";
const ctx = getCtx();

interface Props {
  id: number;
}

const { id } = Astro.props;

const store = {
  name: `dot: ${id}`,
  children: [],
};

ctx.children.push(store);
---
