---
interface Props extends Partial<SVGCircleElement> {
    r?: number;
    c?: { x: number; y: number };
}

const { c = { ...{ x: 0, y: 0 } }, r = 2, ...t } = Astro.props;
---

<circle cx={c.x} cy={c.y} r={r} {...t}></circle>
