---

---

<div class="grid">
    <slot />
</div>

<style>
    .grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(min(384px, 100%), 1fr));
        gap: 8px;
    }
</style>
