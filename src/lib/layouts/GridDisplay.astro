---
import WorksLayout from "./WorksLayout.astro";
---

<div class="grid">
    <slot />
</div>

<style>
    .grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(384px, 1fr));
        gap: 8px;
    }
</style>
