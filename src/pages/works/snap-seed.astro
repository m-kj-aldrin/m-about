---
import GridDisplay from "@lib/layouts/GridDisplay.astro";
import WorksLayout from "@lib/layouts/WorksLayout.astro";
const images = await Astro.glob("/src/assets/seed/img/*").then((files) => {
    return files.map((file) => file.default);
});
---

<WorksLayout title="snap-seed">
    <GridDisplay>
        {
            images.map((img) => {
                return (
                    <img
                        src={img.src}
                        width={1024}
                        height={1024}
                        loading="lazy"
                    />
                );
            })
        }
    </GridDisplay>
</WorksLayout>

<style>
    img {
        height: 100%;
        aspect-ratio: 1/1;
    }
</style>
